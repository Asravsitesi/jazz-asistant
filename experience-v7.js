(()=>{'use strict';let live=false;const esc=v=>String(v||'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])),clean=v=>String(v||'').replace(/[\u0000-\u0008\u000B\u000C\u000E-\u001F\u007F\uFFFD]/g,'').replace(/[\p{Extended_Pictographic}\p{Emoji_Presentation}\uFE0F\u200D]/gu,'').trim();function inline(text){return esc(text).replace(/`([^`]+)`/g,'<code>$1</code>').replace(/\*\*([^*]+)\*\*/g,'<strong>$1</strong>').replace(/__([^_]+)__/g,'<strong>$1</strong>').replace(/\*([^*\n]+)\*/g,'<em>$1</em>').replace(/(https?:\/\/[^\s<]+)/g,'<a href="$1" target="_blank" rel="noopener noreferrer">$1</a>')}function format(value){const codes=[];let text=clean(value).replace(/```(?:\w+)?\n?([\s\S]*?)```/g,(_,code)=>`§§CODE${codes.push('<pre><code>'+esc(code.trim())+'</code></pre>')-1}§§`),out=[],list='';const close=()=>{if(list){out.push(`</${list}>`);list=''}};for(const raw of text.split('\n')){const line=raw.trim(),code=line.match(/^§§CODE(\d+)§§$/);if(code){close();out.push(codes[+code[1]]);continue}if(!line){close();continue}let m;if((m=line.match(/^(#{1,3})\s+(.+)$/))){close();const h=Math.min(4,m[1].length+1);out.push(`<h${h}>${inline(m[2])}</h${h}>`);continue}if((m=line.match(/^[-•*]\s+(.+)$/))){if(list!=='ul'){close();list='ul';out.push('<ul>')}const item=m[1].replace(/^([^:]{1,45}):\s*/,(_,label)=>`**${label}:** `);out.push('<li>'+inline(item)+'</li>');continue}if((m=line.match(/^\d+[.)]\s+(.+)$/))){if(list!=='ol'){close();list='ol';out.push('<ol>')}out.push('<li>'+inline(m[1])+'</li>');continue}if((m=line.match(/^>\s*(.+)$/))){close();out.push('<blockquote>'+inline(m[1])+'</blockquote>');continue}close();out.push('<p>'+inline(line)+'</p>')}close();return out.join('')}function recover(bubble){const temp=document.createElement('div');temp.innerHTML=bubble.innerHTML.replace(/<strong>/gi,'**').replace(/<\/strong>/gi,'**').replace(/<em>/gi,'*').replace(/<\/em>/gi,'*').replace(/<code>/gi,'`').replace(/<\/code>/gi,'`').replace(/<br\s*\/?>/gi,'\n');return clean(temp.textContent)}function enhance(bubble,animate){if(bubble.dataset.v7)return;bubble.dataset.v7='1';const source=recover(bubble);if(!source)return;if(!animate){bubble.innerHTML=format(source);return}bubble.innerHTML='';bubble.classList.add('revealing');const step=Math.max(2,Math.ceil(source.length/190));let index=0;const tick=()=>{if(!bubble.isConnected)return;index=Math.min(source.length,index+step);bubble.innerHTML=format(source.slice(0,index));if(index<source.length)setTimeout(tick,13);else{bubble.innerHTML=format(source);bubble.classList.remove('revealing')}};tick()}function scan(root=document,animate=false){root.querySelectorAll?.('.message.assistant .bubble:not([data-v7])').forEach(b=>enhance(b,animate))}document.addEventListener('DOMContentLoaded',()=>{scan();setTimeout(()=>live=true,900);new MutationObserver(records=>records.forEach(r=>r.addedNodes.forEach(n=>{if(n.nodeType===1)scan(n,live)}))).observe(document.body,{childList:true,subtree:true})});})();
