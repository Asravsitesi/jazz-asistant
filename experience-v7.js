(()=>{'use strict';const scan=(root=document)=>root.querySelectorAll?.('.message.assistant .bubble:not([data-v8])').forEach(b=>{b.dataset.v8='1';const box=document.querySelector('#messages'),article=b.closest('.message');if(box&&article)requestAnimationFrame(()=>box.scrollTo({top:Math.max(0,article.offsetTop-box.clientHeight*.18),behavior:'smooth'}))});const start=()=>{scan();new MutationObserver(records=>records.forEach(r=>r.addedNodes.forEach(n=>{if(n.nodeType===1)scan(n)}))).observe(document.body,{childList:true,subtree:true})};document.readyState==='loading'?document.addEventListener('DOMContentLoaded',start):start()})();